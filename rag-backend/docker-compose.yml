services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage

  # Redis and Postgres are phase 4 in the migration plan — added here
  # commented out so `docker compose up` for phase 1 stays minimal.
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: rag
  #     POSTGRES_USER: rag
  #     POSTGRES_PASSWORD: rag
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

volumes:
  qdrant_data:
  # postgres_data:
